[pytest]
asyncio_mode = auto
markers =
    e2e: end-to-end tests requiring live Postgres, Redis, and MinIO
    integration: integration tests requiring live Postgres (app_user for RLS)
    openai: tests requiring OPENAI_API_KEY and AI_PROVIDER=openai
    local: tests requiring a reachable Ollama instance
testpaths = tests
